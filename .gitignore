# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environment
.venv/
venv/
ENV/

# Environment variables
.env

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/

# OS
.DS_Store
Thumbs.db

# Testing / Coverage
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# Docker volumes
infrastructure/pgdata/

# Backtest runtime artifacts (Phase 1 LLM-mode attribution)
# Saved runs, LLM response cache, and snapshotted skill bundles are
# per-developer state and get regenerated from the source tree.
# Reference data under data/universes/ is NOT ignored.
data/backtest_runs/
data/skill_bundles/
data/llm_response_cache.db
data/llm_response_cache.db-journal
data/llm_response_cache.db-wal
data/llm_response_cache.db-shm
data/noise_floor_cache.db
data/noise_floor_cache.db-journal
data/noise_floor_cache.db-wal
data/noise_floor_cache.db-shm

# Alembic
*.pyc

# Claude Code
.claude/

# Ruff
.ruff_cache/

# Logs
*.log

# Git worktrees
.worktrees/

# Local prod DB dumps (must never be committed by the auto-commit path or a local `git add`)
scheduled_run_results/*_neondb_*.json
# Atomic-write leftovers from a crashed build_report_json (must not be auto-committed)
scheduled_run_results/*.tmp
